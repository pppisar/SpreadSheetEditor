#include "CInterfaceController.h"

void CInterfaceController::action(int actKey) {
    switch (actKey) {
        case 'a':
            break;
        case 'b':
            break;
        //TODO: scroling(KeyUp/KeyDown) and close(ESC)
    }
}